# TOAC (Tower Open API Client)
TOAC 是一个旨在通过OAS(OpenApi Specification)文件自动生成兼容Tower和Hyper生态的客户端代码的工具库。






```rust
impl ::tower_service::Service<CreateResource> for Client {
    fn call() {
        
    }
}
```